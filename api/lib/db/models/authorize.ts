export default interface Authorize {
    authorize_id: string,
    user_id: string,
    ip_log: string,
    access_token: string,
    refresh_token: string,
    created_at: string,
    updated_at: string
}